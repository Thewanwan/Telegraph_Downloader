name: telegraph_downloader
description: Telegraph 图册批量下载工具
publish_to: 'none'
version: 1.0.3+4

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  path: ^1.9.0
  path_provider: ^2.1.0
  html: ^0.15.4
  shared_preferences: ^2.2.0
  provider: ^6.1.0
  file_picker: ^6.1.0
  open_file: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
