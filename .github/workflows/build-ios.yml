name: Build iOS IPA

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.5'
          channel: 'stable'

      - name: Generate iOS project
        run: flutter create --project-name telegraph_downloader --org com.telegraph --platforms=ios --overwrite .

      - name: Install dependencies
        run: flutter pub get

      - name: Build iOS (simulator, profile mode)
        run: flutter build ios --profile --simulator

      - name: Create ZIP
        run: |
          cd build/ios/iosimulator
          zip -r ../../TelegraphDownloader-iOS.zip Runner.app

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: telegraph-downloader-ios
          path: build/TelegraphDownloader-iOS.zip

      - name: Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: build/TelegraphDownloader-iOS.zip
          generate_release_notes: true
